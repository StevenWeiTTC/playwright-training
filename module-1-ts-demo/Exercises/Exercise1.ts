function calculateSquare(length: number) {
    return length*length;
  }

const calculateTriangleArea = (base: number, height: number) => (base*height)/2;

console.log(calculateSquare(5));
console.log(calculateTriangleArea(3,4));